namespace Help.Item
{
    public enum ItemType
    {
        Material,
        Weapon,
        SubWeapon,   // 서브무기/유틸(로프·폭탄 등) — "사용(use)" 액션으로 능력 적용
        HeadArmor,
        BodyArmor,
        LegArmor,
        Accessory,
        Consumable
    }
}
